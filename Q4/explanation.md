# Question 4 Explanation

- I inspected open files and process file descriptors to understand how Linux exposes file access at the OS level.
- I used redirection operators to capture standard output, standard error, and combined output into files for analysis.
- I also reviewed resource limits so the lab report reflects the shell's current I/O-related constraints.
- These observations show how Linux manages file I/O through descriptors, streams, and redirection.
